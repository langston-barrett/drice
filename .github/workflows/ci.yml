name: ci
on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: langston-barrett/gha/.github/workflows/rust-ci.yml@cb3dbd0181f17f451e5bf2b92fdc2a4f51ce185b
    with:
      bench: false
      os: ubuntu-latest
      pre-hook: |
        rustup update nightly
        ./scripts/build.sh

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          persist-credentials: false
      - uses: langston-barrett/gha/.github/actions/install-linters@d0ce6a1c032ebca4fb9c0f8fb0216acafbf5cc7f
      - run: ./scripts/lint/lint.py
